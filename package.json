{
    "version": "0.0.8",
    "scripts": {
        "build": "mkdir -p dist && mkdir -p dist/bin",
        "postbuild": "cp ./action.yml ./dist/action.yml && cp ./.gitignore ./dist/.gitignore && cp ./bin/run.sh ./dist/bin/run.sh && chmod +x ./dist/bin/run.sh",
        "test": "echo 'Noting to test'"
    }
}
